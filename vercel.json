{
  "version": 2,
  "builds": [
    { "src": "server", "use": "@vercel/node" },
    { "src": "src", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server/server.js" },
    { "src": "/(.*)", "dest": "src/$1" }
  ]
}
